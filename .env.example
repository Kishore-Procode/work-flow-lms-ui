# API Configuration
# Base URL for the API endpoints
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Host URL for the API server (used for image URLs and other resources)
VITE_API_HOST=http://localhost:3000

# App Configuration
VITE_APP_NAME=Student-ACT LMS
VITE_APP_VERSION=1.0.0

# Development Configuration
# These are automatically set by Vite based on the mode
# VITE_DEV=true (in development)
# VITE_PROD=true (in production)

# Production Example:
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
# VITE_API_HOST=https://api.yourdomain.com
